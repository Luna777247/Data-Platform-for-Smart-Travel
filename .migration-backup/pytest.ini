[pytest]
# Only run Python tests (avoid collecting log/text artifacts as doctests)
addopts = -p no:doctest
python_files = test_*.py
norecursedirs =
  .git
  .pytest_cache
  infra/logs
  infra/archive
  dags
  docs
  apps/frontend/node_modules
  apps/frontend/.next
  storage

